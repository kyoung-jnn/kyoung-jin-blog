---
title: Nodejs, JavsScript로 백준 풀기
date: '2022-03-16'
summary: JavaScript로 백준을 풀어보자
publish: true
---

**`Python`**, **`C++`** 로 문제를 풀 때는 **백준**을 이용했는데
현재는 **`JavaScript`** 를 사용하여
인풋을 신경 안써도 되는 **프로그래머스**를 이용했습니다..🥲

**알고리즘 종류 별로 카테고리가 존재하는 백준**을 다시 이용하려고
**`Node.js`** 환경으로 알고리즘을 푸는법은 정리했습니다.

> ❗️ **백준, vscode**을 기준으로 합니다.

---

## 1. 인풋 설정

<Image
  alt="list"
  src={`/posts/2022/03/algorithm-nodejs/list.png`}
  width="200"
  height="150"
/>

문제들이 존재하는 폴더에 `input.txt` 란 **텍스트 파일**을 생성하여 **백준 문제들의 인풋**을 입력하고 받습니다.

## 2. 인풋 받기

**백준**은 문제를 푸는 사람이 **인풋에 대한 정보**를 따로 파싱하여 신경써야 합니다.
위에서 지정한 `input.txt` 를 **`fs`** 모듈을 통해 사용합니다.

### 한 줄 입력 받기

```javascript
const fs = require('fs');
// 현재 문제들이 존재하는 곳에서 input.txt 파일 찾기!
// "/dev/stdin" 는 백준에 제출할 때 사용합니다.
const filePath =
  process.platform === 'linux' ? '/dev/stdin' : __dirname + '/input.txt';
let input = fs.readFileSync(filePath).toString().split('\n');

let n = input[0];

function solution(n) {
  // 문제 풀이
}

solution(n);
```

### 여러 줄 입력 받기

```javascript
const fs = require('fs');
const filePath =
  process.platform === 'linux' ? '/dev/stdin' : __dirname + '/input.txt';
let input = fs.readFileSync(filePath).toString().split('\n');

let n = input[0];
let numbers = [];

for (let i = 1; i < input.length; i++) {
  if (input[i] !== '') {
    numbers.push(input[i].split(' '));
  }
}

function solution(n, numbers) {
  // 문제 풀이
}

solution(n, numbers);
```

## 3. 디버깅 하기 (vscode)

코드의 **실행 순서**를 확인하기 위해 디버깅이 필요할 때가 존재합니다.

<Image
  alt="consolelog"
  src={`/posts/2022/03/algorithm-nodejs/console.png`}
  auto
/>

**vscode**를 이용하여 알고리즘 코드의 **중단점**을 지정합니다.

<Image
  alt="vscode"
  src={`/posts/2022/03/algorithm-nodejs/vscode.png`}
  size="small"
/>
**디버깅(`f5`)** 을 실행하면 자동으로 **디버깅**이 시작 됩니다. 시작하기 전 위와
같은 창이 나타나는데, **Node.js**를 사용하기 위해 3번째 **Node.js**를 선택합니다.

> 위와 같은 선택이 귀찮아서 폴더에 `launch.json` 파일을 생성할 수도 있지만
> **매번 달라지는 JavaScript 파일명(Ex: 3123.js, 4211.js)** 업데이트를 해줘야해서
> 위와 같은 방법을 선호합니다. 🤓

import { style } from '@vanilla-extract/css';

// content width: 640px;
export const section = style({
  position: 'relative',
  maxWidth: 684,
  margin: '0 auto',
  marginTop: 60,
  padding: '0 10px',
});
